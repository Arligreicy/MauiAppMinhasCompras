namespace MauiAppMinhasCompras
{
    // Declara a classe AppShell que herda da classe Shell, usada para definir a estrutura de navegação da aplicação
    public partial class AppShell : Shell
    {
        // Construtor da classe AppShell
        public AppShell()
        {
            // Inicializa os componentes da classe definidos no arquivo XAML associado
            InitializeComponent();
        }
    }
}
